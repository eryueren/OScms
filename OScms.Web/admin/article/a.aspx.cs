using System;
using System.IO;
using System.Collections;
using System.Collections.Generic;
using System.Collections.Specialized;
using System.Text.RegularExpressions;
using System.Threading;
using System.Web;
using System.Configuration;
using OS.Common;

namespace OS.Web.admin.article
{
    public partial class a : System.Web.UI.Page
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            //string p = "/aspx/index.aspx";
            //if (File.Exists(Utils.GetMapPath(p.Substring(0, p.IndexOf(".aspx") + 5))))
            //{
            //    Response.Write(Utils.GetMapPath("/aspx/index.aspx"));
            //}




          // Response.Write(str);
     //  Response.Write(Utils.GetMapPath("/aspx/index.aspx"));
          //  Response.Write(HttpContext.Current.Request.Url.Authority + "/aspx/index.aspx");

            //ProgressBar.Start();
            //for (int i = 0; i < 10000; i++)
            //{
            //    ProgressBar.Roll("共生成" + 10000 + "个专题，正在发布" + (i + 1) + "个。", ((i + 1) * 100 / 10000));
            //}
        }
    }
}